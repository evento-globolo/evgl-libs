# Contributing

This repository was bootstrapped for `evento-globolo` as part of the `libs` repo family.

## Local checks

```bash
npm test
```

## Branching

Use `agent/<change-summary>` for generated implementation branches and open draft PRs against `main`.
